---
title: Note-Taking Web Application
description: A web application for managing personal notes with Markdown preview and optional AI titles, built with Angular, MongoDB, and OpenAI.
icon: NotebookPen
---
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { GitHubRepoInfo } from '@/components/docs/GitHubRepoInfo';



## Overview
A note-taking web app for creating, editing, and deleting notes. The editor has a Markdown preview, and it can suggest a title from your content. I built it to try Angular with an Express and MongoDB API, plus Docker and NGINX for deploy.

### AI Features

The app can turn a note into a short title or summary with the OpenAI API.

### Key Features
- Create, edit, and delete notes
- Markdown preview
- Optional AI-generated titles
- Login and registration

### Tech Stack
- Angular
- MongoDB
- Express
- Node.js
- Docker
- NGINX
- OpenAI API

### Deployment

The application is containerized with Docker for consistent deployment across environments. The frontend Angular app and the backend Express API each run in their own containers, orchestrated with Docker Compose. An NGINX reverse proxy sits in front of both services, handling SSL termination, static asset caching, and routing requests to the correct container. This setup ensures the application runs reliably in production while keeping the development environment closely matched to the deployed configuration.

### How to Use

<Steps>
    <Step>
        Register or log in.
    </Step>
    <Step>
        Write a note. You can leave the title empty and let the app suggest one.
    </Step>
    <Step>
        Preview the Markdown, then save. Edit or delete notes as needed.
    </Step>
</Steps>

### Links
- [Live Demo](https://note-taking.ctey.dev)

<GitHubRepoInfo
    owner="chinteyley"
    repo="note_taking"
/>
